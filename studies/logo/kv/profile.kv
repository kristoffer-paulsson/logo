#:import Profile studies.logo.baseclass.profile.Profile

<Profile>:
    name: "profile"
    BoxLayout:
        orientation: "vertical"
        LToolbar:
            title: strings.TEXT_PROFILE
            id: toolbar
            left_action_items: [["menu", lambda x: root.nav_drawer.toggle_nav_drawer()]]
        ScrollView:
            do_scroll_x: False
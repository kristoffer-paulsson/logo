#:import Documents logo.baseclass.documents.Documents

<Documents>:
    name: "documents"
    BoxLayout:
        orientation: "vertical"
        LToolbar:
            title: strings.TEXT_SECTION_DOCUMENTS
            id: toolbar
            left_action_items: [["menu", lambda x: root.nav_drawer.set_state("open")]]
        ScrollView:
            do_scroll_x: False